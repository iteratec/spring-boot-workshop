package de.iteratec.workshop.springdemo.plain;

import org.junit.jupiter.api.Test;

class TextProcessorTest {

    @Test
    void run() {
        // given
        TextProcessor textProcessor = new TextProcessor();
        // when
        textProcessor.run();
        // then
        // TODO is there nothing we can check?!?
    }
}